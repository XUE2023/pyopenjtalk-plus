name: Build Android Wheels (Manual)

on:
  workflow_dispatch:
    inputs:
      python_version:
        description: 'Python version'
        required: true
        default: '3.13'
        type: choice
        options:
          - '3.13'
      android_abi:
        description: 'Android ABI'
        required: true
        default: 'arm64-v8a'
        type: choice
        options:
          - arm64-v8a
          - armeabi-v7a
          - x86_64

jobs:
  build_android_wheels:
    name: Build Android wheels (${{ inputs.android_abi }}, Python ${{ inputs.python_version }})
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository with submodules
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python_version }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build cmake

      - name: Download Android NDK
        run: |
          NDK_VERSION=r26d
          wget https://dl.google.com/android/repository/android-ndk-${NDK_VERSION}-linux.zip
          unzip android-ndk-${NDK_VERSION}-linux.zip
          echo "ANDROID_NDK_HOME=$PWD/android-ndk-${NDK_VERSION}" >> $GITHUB_ENV
          # 将 NDK 工具链加入 PATH
          echo "$PWD/android-ndk-${NDK_VERSION}/toolchains/llvm/prebuilt/linux-x86_64/bin" >> $GITHUB_PATH

      - name: Install Python build dependencies
        run: |
          pip install --upgrade pip
          pip install setuptools wheel cython numpy scikit-build-core

      - name: Build wheel for Android
        env:
          # CMake 工具链文件
          CMAKE_ARGS: "-DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_HOME/build/cmake/android.toolchain.cmake -DANDROID_ABI=${{ inputs.android_abi }} -DANDROID_PLATFORM=24"
          # 强制使用 NDK 的编译器
          CC: "$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/${{ inputs.android_abi == 'arm64-v8a' && 'aarch64' || 'armv7a' }}-linux-android24-clang"
          CXX: "$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/${{ inputs.android_abi == 'arm64-v8a' && 'aarch64' || 'armv7a' }}-linux-android24-clang++"
          # 禁用 pip 隔离构建
          PIP_NO_BUILD_ISOLATION: 1
        run: |
          # 使用 pip wheel 构建（比 python -m build 更可靠）
          pip wheel . --no-build-isolation -w dist

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: pyopenjtalk-plus-android-${{ inputs.android_abi }}-py${{ inputs.python_version }}
          path: dist/*.whl
          retention-days: 7